<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Tasktracker</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <li><a href="/">Return home</a></li>
            <li><%= link "Todo List", to: todo_path(@conn, :index) %></li>
            <li><%= link "Users", to: user_path(@conn, :index) %></li>
            <%#= if assigns[:logged_in] do %>
            <!-- TODO: Add conditional logic to ensure the variable exists? -->
            <%= if @conn.assigns.logged_in == false do %>
            <li>
              <%= link "Register", to: user_path(@conn, :new) %>
            </li>
            <% end %>
            <%= if @conn.assigns.logged_in == false do %>
            <% # this is the is_nil version, to safely see if anything was even put into assigns %>
            <%#= if !is_nil(@conn.assigns[:component_engine]) do %>
            <li>
              <%= link "Login", to: sitesession_path(@conn, :new) %>
            </li>
            <% end %>
            <li><%= link "Logout", to: sitesession_path(@conn, :logout) %></li>

          </ul>
        </nav>
        <span class="logo"></span>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
      <hr />
      <h3 class="text-center">Hello <%= @conn.assigns.plugtest %></h3>
      <h3 class="text-center">Logged in: <%= @conn.assigns.logged_in %></h3>



    </div> <!-- /container -->
    <div>

    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
  <script>
    // this script seems to populate the window object
    console.log("Script being run from layout!");
    var layout_dinosaur = {
      roar: function() { console.log("LAYOUT ROAR!"); },
      stomp: function() { console.log("Too tired to stomp in layout."); }
    }
  </script>
</html>
